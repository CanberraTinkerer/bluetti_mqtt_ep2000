[build-system]
requires = ["setuptools>=40.8.0", "wheel"]
build-backend = "setuptools.build_meta:__legacy__"
[tool.setuptools.package-data]
# This tells python to look inside all packages ("*") and grab any .json files
"*" = ["*.json"]
